<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
			 xmlns:controls="clr-namespace:PrismApp"
             x:Class="PrismApp.Views.MainPage"
             Title="{Binding Title}">

	<ScrollView>
		<controls:RepeaterView ItemsSource="{Binding Items}">
			<controls:RepeaterView.ItemTemplate>
				<DataTemplate>
					<ContentView Padding="5" HeightRequest="100">
						<Button Text="{Binding Title}" Command="{Binding Command}" Margin="5"/>
					</ContentView>
				</DataTemplate>
			</controls:RepeaterView.ItemTemplate>
		</controls:RepeaterView>
	</ScrollView>

</ContentPage>